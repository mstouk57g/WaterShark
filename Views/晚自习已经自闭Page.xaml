<Page
    x:Class="WaterShark.Views.晚自习已经自闭Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <RelativePanel>
            <TextBlock x:Name="min"/>
            <TextBlock x:Name="dot" Text="：" RelativePanel.RightOf="min"/>
            <TextBlock x:Name="sec" RelativePanel.RightOf="dot"/>


            <Button x:Name="Start" Style="{StaticResource AccentButtonStyle}" Content="开始" RelativePanel.Below="min" Click="ClickStart" Margin="0,50,0,0"/>
        </RelativePanel>
    </Grid>
</Page>
